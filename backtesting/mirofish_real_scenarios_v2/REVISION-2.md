# 第二次实现：将校验前移

首轮 v9 三例均生成三条分支，但存在多条无依据百分比、重复剔除比较口径，以及把未发布信息当成失效条件的问题。**小样本内容检查未通过，因此未进入 24 例扩展，也未替换产品默认。** 首轮六次请求、全部原始输出和代码已保留。

修订 v10 沿用主体拆分与可变分支结构，将引用事实中的数值检查以及已调整比较口径检查加入生成校验，失败后最多重写一次。提示同时要求相反公开信息作为失效条件，并约束停牌时序和输入外专名。

所有费用继续记入同一份 `.data/real-scenarios-v2/budget.json`，总限额保持 0.50 美元 / 192 次请求，无额度重置。第二轮在 `revision-2/` 冻结输入与代码，重新完成相同三例配对后再决定是否扩至 24 例。最终比较 v7/v10，并单列首轮失败成本。

数值检查通过率具有规则强制性，不能作为语义质量提高的独立证据。报告必须同时列出首次输出中的问题、拒绝/重写次数、最终失败、保留的分支和逐例内容复核，不得只报告过滤后的零问题。
